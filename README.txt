Create a database with the name "electronix " 
import the db.sql file located at database/db.sql
if you set a password for your localhost database other than "root" then change the database properties in includes/db.php
otherwise keep it same now, run project

admin username is-- electronixadmin
password is-- 12345678
